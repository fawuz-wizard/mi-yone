# MI YONE backend — PINNED to the exact versions the full test suite passed on
# (Python 3.11; see .python-version). A platform build installs precisely this,
# never "whatever is newest today". Upgrade deliberately: bump, run the suite,
# commit the new pins together with the green run.
fastapi==0.141.1
starlette==1.0.0
uvicorn[standard]==0.46.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.19.1
argon2-cffi==25.1.0
pydantic==2.13.3
pydantic-settings==2.14.0
python-multipart==0.0.26
httpx==0.28.1

# test-only
pytest==9.1.1
