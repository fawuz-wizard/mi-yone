"use client";
// Partner (Phase 5 5H) — grounded business companion over verified records.
import { PartnerChat } from "@/features/partner/PartnerChat";

export default function PartnerPage() {
  return <PartnerChat />;
}
